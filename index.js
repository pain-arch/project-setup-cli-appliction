#!/usr/bin/env node

// Require Node.js modules
const fs = require('fs');

// Require third-party modules
const projectStructure = {
    "src": ['index.js'],
    "public": ['index.html', 'style.css']
};

